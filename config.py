# --- WALTER PHYSICAL DIMENSIONS (Meters) ---
WHEEL_BASE_M = 0.27        # Distance between left and right wheels
WHEEL_RADIUS_M = 0.075     # Radius of the wheels (150mm diameter / 2)
ROBOT_RADIUS_M = 0.235     # Physical footprint radius (47cm diameter / 2)
LIDAR_HEIGHT_M = 0.55      # Height of the LiDAR

# Estimated max speed in meters per second at 255 PWM
# (We can fine-tune this later with a stopwatch)
MAX_SPEED_MPS = 0.5
